In backtracking questions:
```
When to explore choices using for-loop, and when not to?
-> 1. When the traversal in the graph/matrix will be in random manner -> NO FOR LOOP.
   2. When traversal in a particular order(say nqueen, col1, col2, so on.. ),= -> USE FOR LOOP
```
